# graphify
- **graphify** (`~/.claude/skills/graphify/SKILL.md`) - any input to knowledge graph. Trigger: `/graphify`
When the user types `/graphify`, invoke the Skill tool with `skill: "graphify"` before doing anything else.

# obsidian-wiki
- **obsidian-wiki** (`~/.claude/skills/obsidian-wiki/SKILL.md`) - LLM-maintained personal knowledge wiki (ingest sources, query, lint). Based on Karpathy's LLM Wiki pattern. Trigger: `/obsidian-wiki`
When the user types `/obsidian-wiki`, invoke the Skill tool with `skill: "obsidian-wiki"` before doing anything else.

# instagram-digital-products
- **instagram-digital-products** (`~/.claude/skills/instagram-digital-products/SKILL.md`) - 7 Instagram prompt templates for digital product creators. Trigger: `/instagram-products`
When the user types `/instagram-products`, invoke the Skill tool with `skill: "instagram-digital-products"` before doing anything else.

# youtube-channel-ai
- **youtube-channel-ai** (`~/.claude/skills/youtube-channel-ai/SKILL.md`) - 3-step AI workflow for building a faceless YouTube channel (Channel Forensics, Viral Title Producer, Script Curator). Trigger: `/youtube-channel`
When the user types `/youtube-channel`, invoke the Skill tool with `skill: "youtube-channel-ai"` before doing anything else.

# claude-efficiency
- **claude-efficiency** (`~/.claude/skills/claude-efficiency/SKILL.md`) - 8 fixes to stop wasting Claude's context window and usage limits. Trigger: `/claude-efficiency`
When the user types `/claude-efficiency`, invoke the Skill tool with `skill: "claude-efficiency"` before doing anything else.

# ux-design
- **ux-design** (`~/.claude/skills/ux-design/SKILL.md`) - 5 Claude skills for UI/UX designers: UX audit, AI chat UI, landing page, case study, React conversion. Trigger: `/ux-design`
When the user types `/ux-design`, invoke the Skill tool with `skill: "ux-design"` before doing anything else.

# build-with-claude-code
- **build-with-claude-code** (`~/.claude/skills/build-with-claude-code/SKILL.md`) - Replace SaaS tools (Calendly, Mailchimp, Shopify, Intercom) with Claude Code builds for $0. Trigger: `/build-with-claude-code`
When the user types `/build-with-claude-code`, invoke the Skill tool with `skill: "build-with-claude-code"` before doing anything else.

# manus-vacu-landing
- **manus-vacu-landing** (`~/.claude/skills/manus-vacu-landing/SKILL.md`) - Full Manus-style prompt for building a dark luxury landing page (VACU brand) in React + Tailwind + Framer Motion. Trigger: `/manus-vacu-landing`
When the user types `/manus-vacu-landing`, invoke the Skill tool with `skill: "manus-vacu-landing"` before doing anything else.

# claude-max-effort
- **claude-max-effort** (`~/.claude/skills/claude-max-effort/SKILL.md`) - Fix Claude's reduced effort with a Custom Instructions trick. Forces deep reasoning on every request. Trigger: `/claude-max-effort`
When the user types `/claude-max-effort`, invoke the Skill tool with `skill: "claude-max-effort"` before doing anything else.


# accessibility-review
- **accessibility-review** (`~/.claude/skills/accessibility-review/SKILL.md`) - Run a WCAG 2.1 AA accessibility audit on a design or page. Trigger: `/accessibility-review`
When the user types `/accessibility-review`, invoke the Skill tool with `skill: "accessibility-review"` before doing anything else.

# account-research
- **account-research** (`~/.claude/skills/account-research/SKILL.md`) - Research a company using Common Room data. Trigger: `/account-research`
When the user types `/account-research`, invoke the Skill tool with `skill: "account-research"` before doing anything else.

# algorithmic-art
- **algorithmic-art** (`~/.claude/skills/algorithmic-art/SKILL.md`) - Creating algorithmic art using p5.js with seeded randomness and interactive parameter exploration. Trigger: `/algorithmic-art`
When the user types `/algorithmic-art`, invoke the Skill tool with `skill: "algorithmic-art"` before doing anything else.

# analyze
- **analyze** (`~/.claude/skills/analyze/SKILL.md`) - Answer data questions — from quick lookups to full analyses. Trigger: `/analyze`
When the user types `/analyze`, invoke the Skill tool with `skill: "analyze"` before doing anything else.

# architecture
- **architecture** (`~/.claude/skills/architecture/SKILL.md`) - Create or evaluate an architecture decision record (ADR). Trigger: `/architecture`
When the user types `/architecture`, invoke the Skill tool with `skill: "architecture"` before doing anything else.

# audit-support
- **audit-support** (`~/.claude/skills/audit-support/SKILL.md`) - Support SOX 404 compliance with control testing methodology, sample selection, and documentation standards. Trigger: `/audit-support`
When the user types `/audit-support`, invoke the Skill tool with `skill: "audit-support"` before doing anything else.

# brand-guidelines
- **brand-guidelines** (`~/.claude/skills/brand-guidelines/SKILL.md`) - Applies brand colors and typography to any artifact. Trigger: `/brand-guidelines`
When the user types `/brand-guidelines`, invoke the Skill tool with `skill: "brand-guidelines"` before doing anything else.

# brand-review
- **brand-review** (`~/.claude/skills/brand-review/SKILL.md`) - Review content against brand voice, style guide, and messaging pillars. Trigger: `/brand-review`
When the user types `/brand-review`, invoke the Skill tool with `skill: "brand-review"` before doing anything else.

# build-dashboard
- **build-dashboard** (`~/.claude/skills/build-dashboard/SKILL.md`) - Build an interactive HTML dashboard with charts, filters, and tables. Trigger: `/build-dashboard`
When the user types `/build-dashboard`, invoke the Skill tool with `skill: "build-dashboard"` before doing anything else.

# call-prep
- **call-prep** (`~/.claude/skills/call-prep/SKILL.md`) - Prepare for a customer or prospect call using signals. Trigger: `/call-prep`
When the user types `/call-prep`, invoke the Skill tool with `skill: "call-prep"` before doing anything else.

# call-summary
- **call-summary** (`~/.claude/skills/call-summary/SKILL.md`) - Process call notes or a transcript — extract action items, draft follow-up email, generate internal summary. Trigger: `/call-summary`
When the user types `/call-summary`, invoke the Skill tool with `skill: "call-summary"` before doing anything else.

# campaign-plan
- **campaign-plan** (`~/.claude/skills/campaign-plan/SKILL.md`) - Generate a full campaign brief with objectives, audience, messaging, channel strategy, and content calendar. Trigger: `/campaign-plan`
When the user types `/campaign-plan`, invoke the Skill tool with `skill: "campaign-plan"` before doing anything else.

# canvas-design
- **canvas-design** (`~/.claude/skills/canvas-design/SKILL.md`) - Create beautiful visual art in .png and .pdf documents using design philosophy. Trigger: `/canvas-design`
When the user types `/canvas-design`, invoke the Skill tool with `skill: "canvas-design"` before doing anything else.

# capacity-plan
- **capacity-plan** (`~/.claude/skills/capacity-plan/SKILL.md`) - Plan resource capacity — workload analysis and utilization forecasting. Trigger: `/capacity-plan`
When the user types `/capacity-plan`, invoke the Skill tool with `skill: "capacity-plan"` before doing anything else.

# change-request
- **change-request** (`~/.claude/skills/change-request/SKILL.md`) - Create a change management request with impact analysis and rollback plan. Trigger: `/change-request`
When the user types `/change-request`, invoke the Skill tool with `skill: "change-request"` before doing anything else.

# close-management
- **close-management** (`~/.claude/skills/close-management/SKILL.md`) - Manage the month-end close process with task sequencing, dependencies, and status tracking. Trigger: `/close-management`
When the user types `/close-management`, invoke the Skill tool with `skill: "close-management"` before doing anything else.

# code-review
- **code-review** (`~/.claude/skills/code-review/SKILL.md`) - Review code changes for security, performance, and correctness. Trigger: `/code-review`
When the user types `/code-review`, invoke the Skill tool with `skill: "code-review"` before doing anything else.

# comp-analysis
- **comp-analysis** (`~/.claude/skills/comp-analysis/SKILL.md`) - Analyze compensation — benchmarking, band placement, and equity modeling. Trigger: `/comp-analysis`
When the user types `/comp-analysis`, invoke the Skill tool with `skill: "comp-analysis"` before doing anything else.

# competitive-brief
- **competitive-brief** (`~/.claude/skills/competitive-brief/SKILL.md`) - Create a competitive analysis brief for one or more competitors or a feature area. Trigger: `/competitive-brief`
When the user types `/competitive-brief`, invoke the Skill tool with `skill: "competitive-brief"` before doing anything else.

# competitive-intelligence
- **competitive-intelligence** (`~/.claude/skills/competitive-intelligence/SKILL.md`) - Research competitors and build an interactive battlecard. Trigger: `/competitive-intelligence`
When the user types `/competitive-intelligence`, invoke the Skill tool with `skill: "competitive-intelligence"` before doing anything else.

# compliance-tracking
- **compliance-tracking** (`~/.claude/skills/compliance-tracking/SKILL.md`) - Track compliance requirements and audit readiness (SOC 2, ISO 27001, GDPR). Trigger: `/compliance-tracking`
When the user types `/compliance-tracking`, invoke the Skill tool with `skill: "compliance-tracking"` before doing anything else.

# compose-outreach
- **compose-outreach** (`~/.claude/skills/compose-outreach/SKILL.md`) - Generate personalized outreach messages using signals. Trigger: `/compose-outreach`
When the user types `/compose-outreach`, invoke the Skill tool with `skill: "compose-outreach"` before doing anything else.

# consolidate-memory
- **consolidate-memory** (`~/.claude/skills/consolidate-memory/SKILL.md`) - Reflective pass over memory files — merge duplicates, fix stale facts, prune the index. Trigger: `/consolidate-memory`
When the user types `/consolidate-memory`, invoke the Skill tool with `skill: "consolidate-memory"` before doing anything else.

# contact-research
- **contact-research** (`~/.claude/skills/contact-research/SKILL.md`) - Research a specific person using Common Room data. Trigger: `/contact-research`
When the user types `/contact-research`, invoke the Skill tool with `skill: "contact-research"` before doing anything else.

# content-creation
- **content-creation** (`~/.claude/skills/content-creation/SKILL.md`) - Draft marketing content across channels — blog posts, social media, email newsletters, landing pages. Trigger: `/content-creation`
When the user types `/content-creation`, invoke the Skill tool with `skill: "content-creation"` before doing anything else.

# conversation-context
- **conversation-context** (`~/.claude/skills/conversation-context/SKILL.md`) - Saves and restores conversation context across sessions. Trigger: `/conversation-context`
When the user types `/conversation-context`, invoke the Skill tool with `skill: "conversation-context"` before doing anything else.

# create-an-asset
- **create-an-asset** (`~/.claude/skills/create-an-asset/SKILL.md`) - Generate tailored sales assets (landing pages, decks, one-pagers) from deal context. Trigger: `/create-an-asset`
When the user types `/create-an-asset`, invoke the Skill tool with `skill: "create-an-asset"` before doing anything else.

# create-viz
- **create-viz** (`~/.claude/skills/create-viz/SKILL.md`) - Create publication-quality visualizations with Python. Trigger: `/create-viz`
When the user types `/create-viz`, invoke the Skill tool with `skill: "create-viz"` before doing anything else.

# daily-briefing
- **daily-briefing** (`~/.claude/skills/daily-briefing/SKILL.md`) - Start your day with a prioritized sales briefing. Trigger: `/daily-briefing`
When the user types `/daily-briefing`, invoke the Skill tool with `skill: "daily-briefing"` before doing anything else.

# data-context-extractor
- **data-context-extractor** (`~/.claude/skills/data-context-extractor/SKILL.md`) - Generate or improve data context documentation for tables and columns. Trigger: `/data-context-extractor`
When the user types `/data-context-extractor`, invoke the Skill tool with `skill: "data-context-extractor"` before doing anything else.

# data-visualization
- **data-visualization** (`~/.claude/skills/data-visualization/SKILL.md`) - Create effective data visualizations with Python (matplotlib, seaborn, plotly). Trigger: `/data-visualization`
When the user types `/data-visualization`, invoke the Skill tool with `skill: "data-visualization"` before doing anything else.

# debug
- **debug** (`~/.claude/skills/debug/SKILL.md`) - Structured debugging session — reproduce, isolate, diagnose, and fix. Trigger: `/debug`
When the user types `/debug`, invoke the Skill tool with `skill: "debug"` before doing anything else.

# deploy-checklist
- **deploy-checklist** (`~/.claude/skills/deploy-checklist/SKILL.md`) - Pre-deployment verification checklist. Trigger: `/deploy-checklist`
When the user types `/deploy-checklist`, invoke the Skill tool with `skill: "deploy-checklist"` before doing anything else.

# design-critique
- **design-critique** (`~/.claude/skills/design-critique/SKILL.md`) - Get structured design feedback on usability, hierarchy, and consistency. Trigger: `/design-critique`
When the user types `/design-critique`, invoke the Skill tool with `skill: "design-critique"` before doing anything else.

# design-handoff
- **design-handoff** (`~/.claude/skills/design-handoff/SKILL.md`) - Generate developer handoff specs from a design. Trigger: `/design-handoff`
When the user types `/design-handoff`, invoke the Skill tool with `skill: "design-handoff"` before doing anything else.

# design-system
- **design-system** (`~/.claude/skills/design-system/SKILL.md`) - Audit, document, or extend your design system. Trigger: `/design-system`
When the user types `/design-system`, invoke the Skill tool with `skill: "design-system"` before doing anything else.

# digest
- **digest** (`~/.claude/skills/digest/SKILL.md`) - Generate a daily or weekly digest of activity across all connected sources. Trigger: `/digest`
When the user types `/digest`, invoke the Skill tool with `skill: "digest"` before doing anything else.

# doc-coauthoring
- **doc-coauthoring** (`~/.claude/skills/doc-coauthoring/SKILL.md`) - Guide users through a structured workflow for co-authoring documentation. Trigger: `/doc-coauthoring`
When the user types `/doc-coauthoring`, invoke the Skill tool with `skill: "doc-coauthoring"` before doing anything else.

# documentation
- **documentation** (`~/.claude/skills/documentation/SKILL.md`) - Write and maintain technical documentation — APIs, architecture docs, runbooks. Trigger: `/documentation`
When the user types `/documentation`, invoke the Skill tool with `skill: "documentation"` before doing anything else.

# docx
- **docx** (`~/.claude/skills/docx/SKILL.md`) - Create, read, edit, or manipulate Word documents (.docx files). Trigger: `/docx`
When the user types `/docx`, invoke the Skill tool with `skill: "docx"` before doing anything else.

# draft-content
- **draft-content** (`~/.claude/skills/draft-content/SKILL.md`) - Draft blog posts, social media, email newsletters, landing pages, press releases. Trigger: `/draft-content`
When the user types `/draft-content`, invoke the Skill tool with `skill: "draft-content"` before doing anything else.

# draft-offer
- **draft-offer** (`~/.claude/skills/draft-offer/SKILL.md`) - Draft an offer letter with comp details and terms. Trigger: `/draft-offer`
When the user types `/draft-offer`, invoke the Skill tool with `skill: "draft-offer"` before doing anything else.

# draft-outreach
- **draft-outreach** (`~/.claude/skills/draft-outreach/SKILL.md`) - Research a prospect then draft personalized outreach. Trigger: `/draft-outreach`
When the user types `/draft-outreach`, invoke the Skill tool with `skill: "draft-outreach"` before doing anything else.

# email-sequence
- **email-sequence** (`~/.claude/skills/email-sequence/SKILL.md`) - Design and draft multi-email sequences with full copy, timing, branching logic. Trigger: `/email-sequence`
When the user types `/email-sequence`, invoke the Skill tool with `skill: "email-sequence"` before doing anything else.

# email-sorter
- **email-sorter** (`~/.claude/skills/email-sorter/SKILL.md`) - Sort and categorize emails automatically. Trigger: `/email-sorter`
When the user types `/email-sorter`, invoke the Skill tool with `skill: "email-sorter"` before doing anything else.

# enrich-lead
- **enrich-lead** (`~/.claude/skills/enrich-lead/SKILL.md`) - Instant lead enrichment — drop a name, company, or email and get a full contact card. Trigger: `/enrich-lead`
When the user types `/enrich-lead`, invoke the Skill tool with `skill: "enrich-lead"` before doing anything else.

# explore-data
- **explore-data** (`~/.claude/skills/explore-data/SKILL.md`) - Profile and explore a dataset to understand its shape, quality, and patterns. Trigger: `/explore-data`
When the user types `/explore-data`, invoke the Skill tool with `skill: "explore-data"` before doing anything else.

# financial-statements
- **financial-statements** (`~/.claude/skills/financial-statements/SKILL.md`) - Generate financial statements (income statement, balance sheet, cash flow). Trigger: `/financial-statements`
When the user types `/financial-statements`, invoke the Skill tool with `skill: "financial-statements"` before doing anything else.

# forecast
- **forecast** (`~/.claude/skills/forecast/SKILL.md`) - Generate a weighted sales forecast with best/likely/worst scenarios. Trigger: `/forecast`
When the user types `/forecast`, invoke the Skill tool with `skill: "forecast"` before doing anything else.

# incident-response
- **incident-response** (`~/.claude/skills/incident-response/SKILL.md`) - Run an incident response workflow — triage, communicate, and write postmortem. Trigger: `/incident-response`
When the user types `/incident-response`, invoke the Skill tool with `skill: "incident-response"` before doing anything else.

# internal-comms
- **internal-comms** (`~/.claude/skills/internal-comms/SKILL.md`) - Write internal communications — status reports, leadership updates, company newsletters, FAQs. Trigger: `/internal-comms`
When the user types `/internal-comms`, invoke the Skill tool with `skill: "internal-comms"` before doing anything else.

# interview-prep
- **interview-prep** (`~/.claude/skills/interview-prep/SKILL.md`) - Create structured interview plans with competency-based questions and scorecards. Trigger: `/interview-prep`
When the user types `/interview-prep`, invoke the Skill tool with `skill: "interview-prep"` before doing anything else.

# journal-entry
- **journal-entry** (`~/.claude/skills/journal-entry/SKILL.md`) - Prepare journal entries with proper debits, credits, and supporting detail. Trigger: `/journal-entry`
When the user types `/journal-entry`, invoke the Skill tool with `skill: "journal-entry"` before doing anything else.

# journal-entry-prep
- **journal-entry-prep** (`~/.claude/skills/journal-entry-prep/SKILL.md`) - Prepare journal entries with proper debits, credits, and supporting documentation for month-end close. Trigger: `/journal-entry-prep`
When the user types `/journal-entry-prep`, invoke the Skill tool with `skill: "journal-entry-prep"` before doing anything else.

# knowledge-synthesis
- **knowledge-synthesis** (`~/.claude/skills/knowledge-synthesis/SKILL.md`) - Combine search results from multiple sources into coherent, deduplicated answers with source attribution. Trigger: `/knowledge-synthesis`
When the user types `/knowledge-synthesis`, invoke the Skill tool with `skill: "knowledge-synthesis"` before doing anything else.

# mcp-builder
- **mcp-builder** (`~/.claude/skills/mcp-builder/SKILL.md`) - Guide for creating high-quality MCP servers that enable LLMs to interact with external services. Trigger: `/mcp-builder`
When the user types `/mcp-builder`, invoke the Skill tool with `skill: "mcp-builder"` before doing anything else.

# memory-management
- **memory-management** (`~/.claude/skills/memory-management/SKILL.md`) - Two-tier memory system — CLAUDE.md for working memory, memory/ directory for the full knowledge base. Trigger: `/memory-management`
When the user types `/memory-management`, invoke the Skill tool with `skill: "memory-management"` before doing anything else.

# metrics-review
- **metrics-review** (`~/.claude/skills/metrics-review/SKILL.md`) - Review and analyze product metrics with trend analysis and actionable insights. Trigger: `/metrics-review`
When the user types `/metrics-review`, invoke the Skill tool with `skill: "metrics-review"` before doing anything else.

# onboarding
- **onboarding** (`~/.claude/skills/onboarding/SKILL.md`) - Generate an onboarding checklist and first-week plan for a new hire. Trigger: `/onboarding`
When the user types `/onboarding`, invoke the Skill tool with `skill: "onboarding"` before doing anything else.

# org-planning
- **org-planning** (`~/.claude/skills/org-planning/SKILL.md`) - Headcount planning, org design, and team structure optimization. Trigger: `/org-planning`
When the user types `/org-planning`, invoke the Skill tool with `skill: "org-planning"` before doing anything else.

# pdf
- **pdf** (`~/.claude/skills/pdf/SKILL.md`) - Read, extract, merge, split, create, or fill PDF files. Trigger: `/pdf`
When the user types `/pdf`, invoke the Skill tool with `skill: "pdf"` before doing anything else.

# people-report
- **people-report** (`~/.claude/skills/people-report/SKILL.md`) - Generate headcount, attrition, diversity, or org health reports. Trigger: `/people-report`
When the user types `/people-report`, invoke the Skill tool with `skill: "people-report"` before doing anything else.

# performance-report
- **performance-report** (`~/.claude/skills/performance-report/SKILL.md`) - Build a marketing performance report with key metrics, trend analysis, wins and misses. Trigger: `/performance-report`
When the user types `/performance-report`, invoke the Skill tool with `skill: "performance-report"` before doing anything else.

# performance-review
- **performance-review** (`~/.claude/skills/performance-review/SKILL.md`) - Structure a performance review with self-assessment, manager template, and calibration prep. Trigger: `/performance-review`
When the user types `/performance-review`, invoke the Skill tool with `skill: "performance-review"` before doing anything else.

# pipeline-review
- **pipeline-review** (`~/.claude/skills/pipeline-review/SKILL.md`) - Analyze pipeline health — prioritize deals, flag risks, get a weekly action plan. Trigger: `/pipeline-review`
When the user types `/pipeline-review`, invoke the Skill tool with `skill: "pipeline-review"` before doing anything else.

# policy-lookup
- **policy-lookup** (`~/.claude/skills/policy-lookup/SKILL.md`) - Find and explain company policies in plain language. Trigger: `/policy-lookup`
When the user types `/policy-lookup`, invoke the Skill tool with `skill: "policy-lookup"` before doing anything else.

# pptx
- **pptx** (`~/.claude/skills/pptx/SKILL.md`) - Create, read, edit, or manipulate PowerPoint files (.pptx). Trigger: `/pptx`
When the user types `/pptx`, invoke the Skill tool with `skill: "pptx"` before doing anything else.

# process-doc
- **process-doc** (`~/.claude/skills/process-doc/SKILL.md`) - Document a business process — flowcharts, RACI, and SOPs. Trigger: `/process-doc`
When the user types `/process-doc`, invoke the Skill tool with `skill: "process-doc"` before doing anything else.

# process-optimization
- **process-optimization** (`~/.claude/skills/process-optimization/SKILL.md`) - Analyze and improve business processes — identify bottlenecks and streamline workflows. Trigger: `/process-optimization`
When the user types `/process-optimization`, invoke the Skill tool with `skill: "process-optimization"` before doing anything else.

# product-brainstorming
- **product-brainstorming** (`~/.claude/skills/product-brainstorming/SKILL.md`) - Brainstorm product ideas, explore problem spaces, and challenge assumptions. Trigger: `/product-brainstorming`
When the user types `/product-brainstorming`, invoke the Skill tool with `skill: "product-brainstorming"` before doing anything else.

# prospect
- **prospect** (`~/.claude/skills/prospect/SKILL.md`) - Full ICP-to-leads pipeline — describe your ideal customer and get ranked enriched leads. Trigger: `/prospect`
When the user types `/prospect`, invoke the Skill tool with `skill: "prospect"` before doing anything else.

# reconciliation
- **reconciliation** (`~/.claude/skills/reconciliation/SKILL.md`) - Reconcile accounts by comparing GL balances to subledgers, bank statements, or third-party data. Trigger: `/reconciliation`
When the user types `/reconciliation`, invoke the Skill tool with `skill: "reconciliation"` before doing anything else.

# recruiting-pipeline
- **recruiting-pipeline** (`~/.claude/skills/recruiting-pipeline/SKILL.md`) - Track and manage recruiting pipeline stages. Trigger: `/recruiting-pipeline`
When the user types `/recruiting-pipeline`, invoke the Skill tool with `skill: "recruiting-pipeline"` before doing anything else.

# research-synthesis
- **research-synthesis** (`~/.claude/skills/research-synthesis/SKILL.md`) - Synthesize user research into themes, insights, and recommendations. Trigger: `/research-synthesis`
When the user types `/research-synthesis`, invoke the Skill tool with `skill: "research-synthesis"` before doing anything else.

# risk-assessment
- **risk-assessment** (`~/.claude/skills/risk-assessment/SKILL.md`) - Identify, assess, and mitigate operational risks. Trigger: `/risk-assessment`
When the user types `/risk-assessment`, invoke the Skill tool with `skill: "risk-assessment"` before doing anything else.

# roadmap-update
- **roadmap-update** (`~/.claude/skills/roadmap-update/SKILL.md`) - Update, create, or reprioritize your product roadmap. Trigger: `/roadmap-update`
When the user types `/roadmap-update`, invoke the Skill tool with `skill: "roadmap-update"` before doing anything else.

# runbook
- **runbook** (`~/.claude/skills/runbook/SKILL.md`) - Create or update an operational runbook for a recurring task or procedure. Trigger: `/runbook`
When the user types `/runbook`, invoke the Skill tool with `skill: "runbook"` before doing anything else.

# schedule
- **schedule** (`~/.claude/skills/schedule/SKILL.md`) - Create a scheduled task that can be run on demand or automatically on an interval. Trigger: `/schedule`
When the user types `/schedule`, invoke the Skill tool with `skill: "schedule"` before doing anything else.

# search
- **search** (`~/.claude/skills/search/SKILL.md`) - Search across all connected sources in one query. Trigger: `/search`
When the user types `/search`, invoke the Skill tool with `skill: "search"` before doing anything else.

# search-strategy
- **search-strategy** (`~/.claude/skills/search-strategy/SKILL.md`) - Query decomposition and multi-source search orchestration. Trigger: `/search-strategy`
When the user types `/search-strategy`, invoke the Skill tool with `skill: "search-strategy"` before doing anything else.

# seo-audit
- **seo-audit** (`~/.claude/skills/seo-audit/SKILL.md`) - Run a comprehensive SEO audit — keyword research, on-page analysis, content gaps, technical checks. Trigger: `/seo-audit`
When the user types `/seo-audit`, invoke the Skill tool with `skill: "seo-audit"` before doing anything else.

# sequence-load
- **sequence-load** (`~/.claude/skills/sequence-load/SKILL.md`) - Find leads matching criteria and bulk-add them to an Apollo outreach sequence. Trigger: `/sequence-load`
When the user types `/sequence-load`, invoke the Skill tool with `skill: "sequence-load"` before doing anything else.

# setup-cowork
- **setup-cowork** (`~/.claude/skills/setup-cowork/SKILL.md`) - Guided Cowork setup — install a matching plugin, try a skill, connect tools. Trigger: `/setup-cowork`
When the user types `/setup-cowork`, invoke the Skill tool with `skill: "setup-cowork"` before doing anything else.

# skill-creator
- **skill-creator** (`~/.claude/skills/skill-creator/SKILL.md`) - Create new skills, modify and improve existing skills, and measure skill performance. Trigger: `/skill-creator`
When the user types `/skill-creator`, invoke the Skill tool with `skill: "skill-creator"` before doing anything else.

# slack-gif-creator
- **slack-gif-creator** (`~/.claude/skills/slack-gif-creator/SKILL.md`) - Create animated GIFs optimized for Slack. Trigger: `/slack-gif-creator`
When the user types `/slack-gif-creator`, invoke the Skill tool with `skill: "slack-gif-creator"` before doing anything else.

# source-management
- **source-management** (`~/.claude/skills/source-management/SKILL.md`) - Manage connected MCP sources for enterprise search. Trigger: `/source-management`
When the user types `/source-management`, invoke the Skill tool with `skill: "source-management"` before doing anything else.

# sox-testing
- **sox-testing** (`~/.claude/skills/sox-testing/SKILL.md`) - Generate SOX sample selections, testing workpapers, and control assessments. Trigger: `/sox-testing`
When the user types `/sox-testing`, invoke the Skill tool with `skill: "sox-testing"` before doing anything else.

# sprint-planning
- **sprint-planning** (`~/.claude/skills/sprint-planning/SKILL.md`) - Plan a sprint — scope work, estimate capacity, set goals, and draft a sprint plan. Trigger: `/sprint-planning`
When the user types `/sprint-planning`, invoke the Skill tool with `skill: "sprint-planning"` before doing anything else.

# sql-queries
- **sql-queries** (`~/.claude/skills/sql-queries/SKILL.md`) - Write correct, performant SQL across all major data warehouse dialects. Trigger: `/sql-queries`
When the user types `/sql-queries`, invoke the Skill tool with `skill: "sql-queries"` before doing anything else.

# stakeholder-update
- **stakeholder-update** (`~/.claude/skills/stakeholder-update/SKILL.md`) - Generate a stakeholder update tailored to audience and cadence. Trigger: `/stakeholder-update`
When the user types `/stakeholder-update`, invoke the Skill tool with `skill: "stakeholder-update"` before doing anything else.

# standup
- **standup** (`~/.claude/skills/standup/SKILL.md`) - Generate a standup update from recent activity. Trigger: `/standup`
When the user types `/standup`, invoke the Skill tool with `skill: "standup"` before doing anything else.

# start
- **start** (`~/.claude/skills/start/SKILL.md`) - Initialize the productivity system and open the dashboard. Trigger: `/start`
When the user types `/start`, invoke the Skill tool with `skill: "start"` before doing anything else.

# statistical-analysis
- **statistical-analysis** (`~/.claude/skills/statistical-analysis/SKILL.md`) - Apply statistical methods including descriptive stats, trend analysis, outlier detection, and hypothesis testing. Trigger: `/statistical-analysis`
When the user types `/statistical-analysis`, invoke the Skill tool with `skill: "statistical-analysis"` before doing anything else.

# status-report
- **status-report** (`~/.claude/skills/status-report/SKILL.md`) - Generate a status report with KPIs, risks, and action items. Trigger: `/status-report`
When the user types `/status-report`, invoke the Skill tool with `skill: "status-report"` before doing anything else.

# synthesize-research
- **synthesize-research** (`~/.claude/skills/synthesize-research/SKILL.md`) - Synthesize user research from interviews, surveys, and feedback into structured insights. Trigger: `/synthesize-research`
When the user types `/synthesize-research`, invoke the Skill tool with `skill: "synthesize-research"` before doing anything else.

# system-design
- **system-design** (`~/.claude/skills/system-design/SKILL.md`) - Design systems, services, and architectures. Trigger: `/system-design`
When the user types `/system-design`, invoke the Skill tool with `skill: "system-design"` before doing anything else.

# task-management
- **task-management** (`~/.claude/skills/task-management/SKILL.md`) - Simple task management using a shared TASKS.md file. Trigger: `/task-management`
When the user types `/task-management`, invoke the Skill tool with `skill: "task-management"` before doing anything else.

# tech-debt
- **tech-debt** (`~/.claude/skills/tech-debt/SKILL.md`) - Identify, categorize, and prioritize technical debt. Trigger: `/tech-debt`
When the user types `/tech-debt`, invoke the Skill tool with `skill: "tech-debt"` before doing anything else.

# template-saver
- **template-saver** (`~/.claude/skills/template-saver/SKILL.md`) - Automatically save and reuse templates across sessions. Trigger: `/template-saver`
When the user types `/template-saver`, invoke the Skill tool with `skill: "template-saver"` before doing anything else.

# testing-strategy
- **testing-strategy** (`~/.claude/skills/testing-strategy/SKILL.md`) - Design test strategies and test plans. Trigger: `/testing-strategy`
When the user types `/testing-strategy`, invoke the Skill tool with `skill: "testing-strategy"` before doing anything else.

# theme-factory
- **theme-factory** (`~/.claude/skills/theme-factory/SKILL.md`) - Toolkit for styling artifacts with a theme — 10 pre-set themes or generate on-the-fly. Trigger: `/theme-factory`
When the user types `/theme-factory`, invoke the Skill tool with `skill: "theme-factory"` before doing anything else.

# update
- **update** (`~/.claude/skills/update/SKILL.md`) - Sync tasks and refresh memory from your current activity. Trigger: `/update`
When the user types `/update`, invoke the Skill tool with `skill: "update"` before doing anything else.

# user-research
- **user-research** (`~/.claude/skills/user-research/SKILL.md`) - Plan, conduct, and synthesize user research. Trigger: `/user-research`
When the user types `/user-research`, invoke the Skill tool with `skill: "user-research"` before doing anything else.

# ux-copy
- **ux-copy** (`~/.claude/skills/ux-copy/SKILL.md`) - Write or review UX copy — microcopy, error messages, empty states, CTAs. Trigger: `/ux-copy`
When the user types `/ux-copy`, invoke the Skill tool with `skill: "ux-copy"` before doing anything else.

# validate-data
- **validate-data** (`~/.claude/skills/validate-data/SKILL.md`) - QA an analysis before sharing — methodology, accuracy, and bias checks. Trigger: `/validate-data`
When the user types `/validate-data`, invoke the Skill tool with `skill: "validate-data"` before doing anything else.

# variance-analysis
- **variance-analysis** (`~/.claude/skills/variance-analysis/SKILL.md`) - Decompose financial variances into drivers with narrative explanations and waterfall analysis. Trigger: `/variance-analysis`
When the user types `/variance-analysis`, invoke the Skill tool with `skill: "variance-analysis"` before doing anything else.

# vendor-review
- **vendor-review** (`~/.claude/skills/vendor-review/SKILL.md`) - Evaluate a vendor — cost analysis, risk assessment, and recommendation. Trigger: `/vendor-review`
When the user types `/vendor-review`, invoke the Skill tool with `skill: "vendor-review"` before doing anything else.

# web-artifacts-builder
- **web-artifacts-builder** (`~/.claude/skills/web-artifacts-builder/SKILL.md`) - Suite of tools for creating elaborate multi-component HTML artifacts using React, Tailwind CSS, shadcn/ui. Trigger: `/web-artifacts-builder`
When the user types `/web-artifacts-builder`, invoke the Skill tool with `skill: "web-artifacts-builder"` before doing anything else.

# weekly-prep-brief
- **weekly-prep-brief** (`~/.claude/skills/weekly-prep-brief/SKILL.md`) - Generate a comprehensive weekly briefing for all external calls in the next 7 days. Trigger: `/weekly-prep-brief`
When the user types `/weekly-prep-brief`, invoke the Skill tool with `skill: "weekly-prep-brief"` before doing anything else.

# write-query
- **write-query** (`~/.claude/skills/write-query/SKILL.md`) - Write optimized SQL for your dialect with best practices. Trigger: `/write-query`
When the user types `/write-query`, invoke the Skill tool with `skill: "write-query"` before doing anything else.

# write-spec
- **write-spec** (`~/.claude/skills/write-spec/SKILL.md`) - Write a feature spec or PRD from a problem statement or feature idea. Trigger: `/write-spec`
When the user types `/write-spec`, invoke the Skill tool with `skill: "write-spec"` before doing anything else.

# xlsx
- **xlsx** (`~/.claude/skills/xlsx/SKILL.md`) - Open, read, edit, create, or convert spreadsheet files (.xlsx, .xlsm, .csv, .tsv). Trigger: `/xlsx`
When the user types `/xlsx`, invoke the Skill tool with `skill: "xlsx"` before doing anything else.

## Context Navigation — Globale Regeln (alle Projekte)

**WICHTIG: Token-Sparregel — immer zuerst die Map lesen, nie blind alle Dateien.**

1. Wenn `graphify-out/index.md` im Projekt existiert → **zuerst diese Datei lesen**, bevor du irgendwelche anderen Dateien öffnest
2. Wenn `graphify-out/wiki/index.md` existiert → diese für Navigation verwenden
3. Rohdateien (`.py`, `.ts`, `.js` etc.) **nur lesen wenn explizit danach gefragt**
4. Architektur- und Codebase-Fragen → immer aus der Map beantworten, nicht durch direktes Lesen
5. Nach Code-Änderungen → Map mit `/graphify .` aktualisieren
